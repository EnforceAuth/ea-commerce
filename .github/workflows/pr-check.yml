# On pull requests, lint changed Rego files.

name: PR Check

on:
  pull_request:
    branches: [main]

jobs:
  lint:
    name: Rego Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: ./.github/actions/rego-lint
